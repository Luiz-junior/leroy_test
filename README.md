Estrutura Base para meus projetos em React com Redux

Primeiro baixe as dependências com o comando:

#### `npm i` ou `yarn install`

Para rodar o projeto basta rodar o comando:

### `npm start` ou `yarn start`

